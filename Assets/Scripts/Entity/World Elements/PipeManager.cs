using UnityEngine;

public class PipeManager : MonoBehaviour {
    public bool entryAllowed = true, bottom = false, miniOnly = false, right, left, instant = false;
    public PipeManager otherPipe;
}
